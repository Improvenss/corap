#include "push_swap.h"

void	sort_many(t_base *base)
{
	find_sml(base); // small middle large sayiyi bul.
	sort_seperate(base, base->size_a);
	// print_stack(base);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// pb(base, false);
	// // pa(base, false);
	// // pa(base, false);
	// // ra(base, false);
	// // ra(base, false);
	// print_stack(base);
}